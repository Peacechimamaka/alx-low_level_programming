#include "main.h"

/**
 * _abs - the function that computes the absolute value of an integer
 * @n: The integer for which to be used
 * Return: The actual value of the integer
 */

int _abs(int n)
{
if (n < 0)
	{
return (-n);
	}
	else
{
	return (n);
}
}

